export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in_progress',
  DEV_TEST = 'dev_test',
  QA_TEST = 'qa_test',
  DONE = 'done',
}

export enum TaskCriticity {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}
